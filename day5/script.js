function k(r)
    {
        return 2 * Math.PI * r;
    }
    console.log("\n");
    console.log(k(1));
    console.log(k(1.5));

    funtion 1(a,b,c)
    {
        var d = b**2 - 4 * a * c;
        if (d>=0);
        {
            var srd = Math.sqrt(d);
            return (-1 * b + srd)/(2*a);
        }
        else
        {
            return NaN
        }

    }

    